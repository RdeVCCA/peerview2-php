{% for note in notes %}
<div>
    <a href="{{ path('note_preview', {'id': note.id}) }}"><h1>{{ note.title|e }}</h1></a>
    <p>by {{ note.username|join(', ') }}</p>
    <p>{{ note.description|e }}</p>
</div>
{% endfor %}